name: Dependency build for Ubuntu 18

on:
  push:
    paths:
      - '.github/workflows/build-depends-18.yml'
      - 'scripts/simple-build-dependencies.sh'
      - 'patch/translations.patch'
  pull_request:
    paths:
      - '.github/workflows/build-depends-18.yml'
      - 'scripts/simple-build-dependencies.sh'
      - 'patch/translations.patch'
  workflow_dispatch: 

jobs:
  build-and-package:
    runs-on: ubuntu-24.04
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Install Container Build Dependencies
        run: |
          docker run --rm \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ubuntu:18.04 \
            /bin/bash -c "
              cd /
              cd /workspace
              apt-get update
              DEBIAN_FRONTEND=noninteractive apt-get install -y apt-transport-https ca-certificates gnupg software-properties-common wget
              wget -nv -O - https://apt.kitware.com/keys/kitware-archive-latest.asc 2>/dev/null | gpg --dearmor - | sudo tee /etc/apt/trusted.gpg.d/kitware.gpg >/dev/null
              apt-add-repository 'deb https://apt.kitware.com/ubuntu/ bionic main'
              DEBIAN_FRONTEND=noninteractive add-apt-repository ppa:ubuntu-toolchain-r/test -y
              DEBIAN_FRONTEND=noninteractive apt-get update
              DEBIAN_FRONTEND=noninteractive apt-get install -y gcc-13 g++-13 \
                build-essential git wget file pkg-config fuse libfuse2 libsecret-1-dev \
                libtool autoconf automake make perl lzip \
                python3 python3-pip python3-setuptools ninja-build libnotify-dev \
                libopenjp2-7-dev libgtk-3-dev libpng-dev zlib1g-dev libtiff-dev libjpeg-dev libgif-dev libwebp-dev libopenblas-dev libglib2.0-dev
              pip3 install meson==0.49.0

              wget -nv https://github.com/Kitware/CMake/releases/download/v3.17.4/cmake-3.17.4-Linux-x86_64.tar.gz
              tar -xf cmake-3.17.4-Linux-x86_64.tar.gz --strip-components=1 -C /usr/local
              
              update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-13 100
              update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-13 100 
              echo \"------------------------------------\"
              echo \"------------------------------------\"
              echo \"------------------------------------\"
              g++ --version
              cmake --version
              meson --version
              cd /workspace/scripts/
              ./simple-build-dependencies.sh
              echo \"----------------------\"
              echo \"----------------------\"
              echo \"----------------------\"
              export PKG_CONFIG_PATH=\"/workspace/build/linux/lib/pkgconfig:$PKG_CONFIG_PATH\"
              make build
              make compile-po
            " 

      - name: Prepare artifact structure
        run: |
          mkdir -p artifact_stage/build
          cp -r build/linux artifact_stage/build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dependencies-ubuntu18-${{ hashFiles('scripts/simple-build-dependencies.sh', 'patch/translations.patch' , '.github/workflows/build-depends-18.yml') }}
          path: |
            artifact_stage
          if-no-files-found: error


